{
  "name": "@preflight/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "Preflight CLI: pre-flight a dependency manifest for CVEs, framework-lockstep, and auto-update safety.",
  "license": "MIT",
  "keywords": ["dependencies", "security", "vulnerabilities", "osv", "supply-chain", "lockstep", "cli"],
  "bin": {
    "preflight": "./dist/index.js"
  },
  "files": ["dist"],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ali0600/preflight.git",
    "directory": "packages/cli"
  },
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@preflight/core": "*"
  }
}
