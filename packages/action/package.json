{
  "name": "@preflight/action",
  "version": "0.1.0",
  "description": "Preflight GitHub Action: pre-flight a PR's dependency changes for CVEs, framework-lockstep, and auto-update safety.",
  "private": true,
  "scripts": {
    "build": "tsup"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1"
  },
  "devDependencies": {
    "@preflight/core": "*"
  }
}
